name: CI

on:
  push:
    branches: [main]
  pull_request:

# Least-privilege GITHUB_TOKEN. This job only checks out the tree and runs the
# validator, so read access to contents is all it needs.
permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: "20"
      - run: node scripts/validate-plugin.mjs
      # Regression tests for the portability checks themselves (dependency-free).
      - run: node --test scripts/tests/*.test.mjs
